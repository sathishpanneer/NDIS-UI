export interface client {
    clientName: string;
    clientID: number;
    homeAddress: string;
    contact: string;
    numberOfTrips: number;
    highlighted?: boolean;
    hovered?: boolean;
  }
  